package com.himanshoe.charty.bar.internal.bar.normalizedhorizontal

/**
 * Five divisions of a 0–100% axis, which lands the labels on the twenties readers expect rather than
 * on the sixths the library's usual default would produce.
 */
internal const val NORMALIZED_HORIZONTAL_AXIS_STEPS = 5
